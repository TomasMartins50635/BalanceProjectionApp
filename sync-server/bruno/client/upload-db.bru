meta {
  name: POST /upload
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/upload
  body: multipartForm
  auth: none
}

headers {
  X-Api-Key: {{apiKey}}
}

body:multipart-form {
  database: @file()
}
