{
  "name": "mobile",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cross-env CAPACITOR_DEV=true npx cap run android",
    "dev:ios": "cross-env CAPACITOR_DEV=true npx cap run ios",
    "build:android": "npm run build:web && npx cap sync android && npx cap open android",
    "build:ios": "npm run build:web && npx cap sync ios && npx cap open ios",
    "build:web": "cd ../web && npm run build",
    "sync": "npx cap sync",
    "sync:dev": "cross-env CAPACITOR_DEV=true npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "cross-env": "^7.0.3",
    "typescript": "~5.8.0"
  }
}
